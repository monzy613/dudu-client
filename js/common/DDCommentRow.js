/**
 * @providesModule DDCommentRow
 */

import React, { Component } from 'react'
import {
  View,
  StyleSheet,
} from 'react-native'

export default class DDCommentRow extends Component {
  render = () => {
  }
}

const styles = StyleSheet.create({
})
